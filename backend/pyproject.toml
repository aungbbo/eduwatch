[project]
name = "eduwatch-backend"
version = "0.1.0"
description = "FastAPI backend for EduWatch"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "pydantic",
  "python-dotenv",
  "groq>=1.2.0",
  "chromadb>=1.5.9",
]

